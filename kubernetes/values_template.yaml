namespace: "geodata"
agentContainer: "prefecthq/prefect:2.7.3-python3.8"

prefect:
  apiURL: "[YOUR_API_URL]"
  apiKey: "[YOUR_API_KEY]"
  workQueue: "geodata"

dev:
  enabled: false
  # likely no need to change this. see info in /dev/README.md
  localDir: /tmp/data/
